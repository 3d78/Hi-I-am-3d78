| Categoria         | Comando / Palavra-chave                       | Função                                |
| ----------------- | --------------------------------------------- | ------------------------------------- |
| Controle de fluxo | `if`, `elif`, `else`                          | Condições                             |
| Laços             | `for`, `while`, `break`, `continue`, `pass`   | Repetição e controle de fluxo         |
| Funções           | `def`, `return`, `yield`                      | Criação e retorno de funções          |
| Classes           | `class`                                       | Declaração de classes (POO)           |
| Exceções          | `try`, `except`, `finally`, `raise`, `assert` | Tratamento de erros                   |
| Importação        | `import`, `from`, `as`                        | Importar módulos                      |
| Escopo            | `global`, `nonlocal`                          | Controle de variáveis globais/locais  |
| Estruturas        | `with`, `lambda`, `del`                       | Contextos, funções anônimas, exclusão |
| Tipos             | `True`, `False`, `None`                       | Valores especiais e booleanos         |


* math – funções matemáticas

* random – geração de números aleatórios

* datetime – datas e horas

* os, sys – sistema operacional

* json, csv, re – manipulação de dados e expressões regulares

* itertools, functools, collections – utilitários avançados

* asyncio, threading, multiprocessing – concorrência e paralelismo
